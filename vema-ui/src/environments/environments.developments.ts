// environment.development.ts (for development)
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000', // Default Django dev server URL
  stripePublicKey: 'YOUR_STRIPE_PUBLIC_KEY', // Replace with your Stripe public key
};
