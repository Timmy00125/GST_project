export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8000', // Replace with your production backend URL
  stripePublicKey: 'YOUR_STRIPE_PUBLIC_KEY', // Replace with your Stripe public key
};
